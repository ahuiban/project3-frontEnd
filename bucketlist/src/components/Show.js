import React from "react";

class Show extends React.Component {
  render() {
    return (
      <div>
        {/* Show a created bucket list, ability to add more to list, check off finished items */}
      </div>
    );
  }
}

export default Show;
